pre-commit:
    parallel: true
    commands:
        versions:
            glob: 'package.json'
            run: pnpm update-versions && git add README.md
        lint-staged:
            glob: '*.{js,ts,jsx,tsx}'
            run: pnpm exec lint-staged
        test:
            glob: 'src/**/*.{js,ts,jsx,tsx}'
            run: pnpm test --passWithNoTests

commit-msg:
    commands:
        commitlint:
            run: pnpm exec commitlint --edit .git/COMMIT_EDITMSG
